// This rendering relies on KeleView.js and Kele.js
// This version currently does not work at all.

const Kele = require('./Kele.js')
const KeleView = require('./KeleView.js')

const keleView = new KeleView()
await keleView.initalize()
